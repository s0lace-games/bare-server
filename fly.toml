app = "bare-server"
primary_region = "ord"

[build]
dockerfile = "Dockerfile"

[http_service]
auto_start_machines = true
auto_stop_machines = false
force_https = true
internal_port = 8_080
min_machines_running = 1

[[vm]]
cpu_kind = "shared"
cpus = 1
memory = "256mb"
